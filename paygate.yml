request:
  styles: [widest]
  desc: ""
  tag: form
  attr:
    action: "https://www.paygate.co.za/paywebv2/process.trans"
    method: POST
  text:
    - type: hidden
    - PAYGATE_ID
    - REFERENCE
    - AMOUNT
    - CURRENCY
    - RETURN_URL
    - TRANSACTION_DATE
    - EMAIL
    - CHECKSUM
  values:
    - read_session: [paygate_amount,email]
    - sql_insert: [payment, payer_id: $pid, user_id: $uid, amount: $paygate_amount, status: pend, method: creditc ]
    - sql_values: select now() TRANSACTION_DATE
    - read_values:
        PAYGATE_ID: $paygate_id
        REFERENCE: $new_payment_id
        AMOUNT: $paygate_amount
        CURRENCY: ZAR
        RETURN_URL: $server_host/?path=paygate/response
        EMAIL: $email
        SECRET: $paygate_secret
    - sql_values: >
        select md5('$PAYGATE_ID|$REFERENCE|$AMOUNT|$CURRENCY|$RETURN_URL|$TRANSACTION_DATE|$EMAIL|$SECRET') CHECKSUM
    - trigger: .submit
    - clear_values: [paygate_amount]

response:
  type: input_page
  name: Complete Electronic Payment
  desc: >
    This the the result of the Paygate process:
  inputs:
    - type: info_text
    - amount
    - status
    - result_description
    - authorisation_code
    - reference
  values:
    - read_session
    - sql_values: select round($paygate_amount/100,2) amount, if ($paygate_status=1,'Success', 'Failed') status
    - let:
        result_description: $paygate_result
        authorisation_code: $paygate_auth_code
        reference: $paygate_trx_id
    - clear_session: [paygate_amount, paygate_status, paygate_result, paygate_auth_code, paygate_trx_id]
  read:
    #- paygate::verify(request)
    - write_session:
        - paygate_status: $TRANSACTION_STATUS
        - paygate_result: $RESULT_DESC
        - paygate_auth_code: $AUTH_CODE
        - paygate_trx_id: $TRANSACTION_ID
        - paygate_amount: $AMOUNT
    - sql_values: >
        select if('$TRANSACTION_STATUS'='1', 'appr', 'reje'), status, id payment_id, payer_id partner_id
        from payment where id = '$REFERENCE'
    - sql_update: [payment, id: $REFERENCE, status, reference1: $TRANSACTION_ID, reference2: $AUTH_CODE ]


  audit:
    partner_id: $payer_id
    detail: >
      Transaction ID: $TRANSACTION_ID Auth Code: $AUTH_CODE Amount: $AMOUNT Result: $RESULT_DESC
