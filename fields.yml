## YAML Template.
---
actions:
 - type: submit

add_user: 
  access: admin
  action:
    dialog: /user/create
country_of_origin:
  name: country
  desc: country where game was developed
audit_trail:
  access: admin
  action:
    url: /user/audit_trail

cellphone:
  desc: The cellphone number at which you can be contacted on
  valid: national_tel

change_password: 
  desc: Change your current password
  access: reg
  action:
    #call: /user/start_reset_pw
    dialog: /user/start_reset_password

    
contacts:
  email: support@mukoni.co.za

confirm_password: 
  type: password
  desc: Type in the same password as above to confirm correctness
  valid: matches(password)

country:
  options:    
    - sql: SELECT item_code,item_name item_code from ref_list WHERE list_name = "country"
  valid: provided
  
create_time:

date:

developed_by:
  name: "developed by:"
  
  

distributor_type:
  options:
    - sql: >
          SELECT item_code, item_name, list_name
          FROM ref_list
          WHERE list_name LIKE "%distr%" 
  
email:
  desc: Valid e-mail address
  valid: email

email_address:
  desc: Valid e-mail address 
  valid: email

error_page:
  desc: Unexpected error occured. Please report to your System Administrator.
 
first_name:
  desc: Your first name as written in your ID book
  valid:
    - at_least(2)
    - alphabetic
  
facebook:
  src: /fb.png
  action:
    url: http://facebook.com
  
goto:
  action: $page.html
 
home:
  action:
    url: /home
     
id:
  name: Identity
  
id_no:
  name: ID Number

id_number:
  name: ID Number
  desc: Valid South African ID number
  
inputs:
  - type: input
  
last_name:
  desc: Your last name as written in your ID book
  valid:
    - at_least(2)
    - alphabetic

latitude:
  valid: /^\d+\.\d+$/


login:
  desc: Log in
  access: public
  audit: $email
  action:
    dialog: /user/login


logout:
  desc: Log out
  access: reg 
  audit: 
  action:
    call: user::logout()

longitude:
  valid: /^\d+\.\d+$/
  
mukoni_logo:
    desc: Visit our home page
    type: image
    src: mukoni.png
    action:
        url: http://www.mukoni.co.za
       
  
otp:
  name: OTP
  desc: One Time Pin

password:
  desc: A secret combination of letters and number only known to you. Please do not give out your password.
  valid: password
 
query:
  type: button
  action:
    call: $class::$invoker($key)
  
register:
  desc: Click here to register
  access: public
  action:
    dialog: /user/register
    
    
submit:
  type: button
  action:
    post: "#page *"
    validate: all
    call: $class::$invoker($key)

submit_page:
  type: button
  name: Submit
  action:
    post: "#page *"
    validate: all
    call: $class::$page($key)
      
title:
  options:
    - Dr
    - Miss
    - Mr
    - Mrs
    - Ms
    - Prof
    - Rev
    
twitter:
  src: /twitter.png
  action:
    url: https://twitter.com

users:
  access: admin
  action:
    url: /user/list

validator:
  post: #page *
  validate: all

view_profile: 
  access: reg
  action:
    dialog: /user/profile

view_users:
  access: admin
  action:
    url: /user/list

